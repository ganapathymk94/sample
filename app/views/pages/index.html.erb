<h2> Hello Guest...!</h2>
<h3> Welcome to my new website...:D</h3>

<h3>Press above tabs to check the functionalities..:)</h3>


<div class="well">
        <h4>Recent Posts</h4>
    
    <% @articles.each do |a| %>
    
       Posted by: <%= a.user.username %> <br />
        <strong><%= link_to a.title,a %></strong>
        <p><%= truncate(a.body) %></p>
        
    <% end %>
    
</div>